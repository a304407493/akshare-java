# 500错误修复说明

## 问题原因

后端API返回500错误是因为CORS配置问题：

```
java.lang.IllegalArgumentException: When allowCredentials is true, allowedOrigins cannot contain the special value "*"
```

## 已修复的内容

已修改 `akshare-web/src/main/java/com/akshare/web/config/CorsConfig.java`：

- 将 `allowCredentials(true)` 改为 `allowCredentials(false)`

## 重启后端服务步骤

### 方法一：使用IDE（推荐）

1. 在IDE中停止当前运行的后端服务
2. 重新运行 `AkshareWebApplication.java`

### 方法二：使用Maven命令

1. 停止当前运行的后端进程（Ctrl+C）
2. 重新编译并启动：

```bash
cd akshare-web
mvn clean spring-boot:run
```

## 验证修复

重启后端服务后，在浏览器中刷新 http://localhost:9876，各个菜单应该可以正常加载数据了。

## 端口配置

- **后端**：8765
- **前端**：9876

## 测试API

可以直接访问以下URL测试后端API：

- http://localhost:8765/api/stock/realtime/em
- http://localhost:8765/api/stock/list
