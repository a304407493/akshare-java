# AKShare Web 快速启动指南

## 端口配置

为避免端口冲突，已配置以下端口：
- **后端服务**：8765
- **前端服务**：9876

## 启动步骤

### 第一步：启动后端服务

1. 打开命令行，进入 `akshare-web` 目录
2. 运行以下命令启动后端：

```bash
cd akshare-web
# Windows用户可以直接双击运行 start-backend.bat
# 或者使用Maven命令：
set MAVEN_OPTS=-Xmx512m -Xms256m
mvn spring-boot:run -Dspring-boot.run.jvmArguments="-Xmx512m -Xms256m"
```

后端服务将在 `http://localhost:8765` 启动

### 第二步：启动前端服务

1. 打开新的命令行窗口，进入前端目录
2. 首次运行需要安装依赖：

```bash
cd akshare-web/src/main/frontend
npm install
```

3. 启动前端服务：

```bash
# Windows用户可以直接双击运行 start-frontend.bat
# 或者使用npm命令：
npm run dev
```

前端服务将在 `http://localhost:9876` 启动

### 第三步：访问应用

在浏览器中打开：`http://localhost:9876`

## 功能页面

- **实时行情**：查看A股实时行情数据
- **历史K线**：查看和分析股票历史K线图
- **股票列表**：浏览所有A股股票列表
- **市场概况**：查看交易所市场概况和个股资料

## 内存配置

如果遇到内存不足的问题，可以调整以下参数：

- 修改 `start-backend.bat` 中的 `-Xmx` 和 `-Xms` 参数
- 例如：`-Xmx1024m -Xms512m` 表示最大1GB，初始512MB

## 注意事项

1. 确保已安装 Java 8+ 和 Maven 3.6+
2. 确保已安装 Node.js 12+ 和 npm
3. 后端和前端需要同时启动才能正常使用
4. 首次运行前端需要执行 `npm install` 安装依赖
5. 数据仅供学习研究使用，不构成投资建议
